---
import ProjectCard from "../components/ProjectCard.astro";

const projects = [
  {
    id: "safehaven",
    title: "Project Safe Heaven: Bachelor Projekt",
    year: "2026",
    season: "Sommer",
    semester: "6",
    course: "Bachelor Projekt",
    description: "Work in Progress. Puzzle-Plattformer im 2D-Pixel-Stil, entwickelt in Godot. Das Spiel thematisiert mentale Zustaende, das Herauskaempfen aus einem inneren Tief und die Verarbeitung von Emotionen. Verschiedene Level stehen fuer unterschiedliche emotionale Welten, von dunklen Hoehlensystemen bis zu ruhigen Schneelandschaften. Pixel-Art-Assets wurden in Aseprite erstellt. Entstanden als Bachelor Projekt im sechsten Semester.",
    categories: ["Game", "2D"],
    tools: ["Godot", "GDScript", "Aseprite"],
    contributors: ["Reshi von Oldenburg", "Moritz Eismann", "Florian Cenzolo"],
    images: [
      "/safehaven/level-dunkel.png",
      "/safehaven/level-feind.png",
      "/safehaven/level-pendel.png",
      "/safehaven/level-wippe.png",
      "/safehaven/level-hoehle.png",
      "/safehaven/level-schnee.png",
      "/safehaven/level-portal.png",
    ],
    videos: [],
  },
  {
    id: "todoapp",
    title: "Todo App: Webapplikation",
    year: "2026",
    season: "Sommer",
    semester: "6",
    course: "Webapps mit TypeScript und React",
    description: "Fullstack-Todo-App als Pruefungsleistung im Kurs 'Webapps mit TypeScript und React'. Die App ermoeglicht mehrere Todo-Listen mit Kategorien, Todos mit Prioritaet, Faelligkeitsdatum, Tags und Sub-Todos, Bulk-Aktionen, Undo-Funktion, Dark/Light-Mode, Mehrsprachigkeit (DE/EN/FR) sowie Such- und Filterfunktion.",
    categories: ["Web", "App"],
    tools: ["Next.js", "React", "TypeScript", "MUI", "Drizzle ORM", "TanStack Query", "SQLite"],
    contributors: ["Niclas Merkel"],
    images: [
      "/todoapp/listen-dark.png",
      "/todoapp/karten-dark.png",
      "/todoapp/detail-light.png",
      "/todoapp/todo-detail.png",
      "/todoapp/schnell-hinzufuegen.png",
      "/todoapp/neue-liste-modal.png",
      "/todoapp/about.png",
    ],
    videos: ["/todoapp/demo.mp4"],
  },
  {
    id: "jankyjenga",
    title: "Janky Jenga: VR/PC Game",
    year: "2025",
    season: "Winter",
    semester: "5",
    course: "Virtuelle und Erweiterte Realität",
    description: "Entwicklung eines asymmetrischen Multiplayer-Spiels in Unity mit OpenXR: Ein VR-Spieler zieht mit den Motion-Controllern physikalisch simulierte Jenga-Bloecke aus einem Turm, waehrend ein PC-Spieler den Turm mit Angriffen destabilisiert. Die Physik-Simulation basiert auf realistischen Rigidbody-Interaktionen. 3D-Modelle wurden in Blender erstellt. Das Projekt entstand als Gruppenarbeit und wurde mit einem Poster und Trailer-Video praesentiert.",
    categories: ["3D", "Game", "VR"],
    tools: ["Unity", "C#", "Blender", "OpenXR"],
    contributors: ["Leonhard Bünger", "Niclas Merkel", "Reshi von Oldenburg"],
    images: [
      "/jankyjenga/pc-ingame.png",
      "/jankyjenga/vr-ingame.png",
      "/jankyjenga/turmeinsturz-vr.png",
      "/jankyjenga/pc-vr-zusammen.png",
    ],
    videos: ["/jankyjenga/trailer.mp4"],
  },
  {
    id: "clockworkcms",
    title: "Clockwork Frontiers: CMS",
    year: "2025",
    season: "Winter",
    semester: "5",
    course: "WWW-Programmierung",
    description: "Erweiterung der Clockwork-Frontiers-Webseite um ein vollstaendiges Content-Management-System. Das CMS ermoeglicht Login und Benutzerverwaltung, dynamisch gerenderte Seiten per Templating, einen blockbasierten Seiteneditor sowie die Verwaltung von Devlogs, Tutorials und Tipps mit Such- und Filterfunktion. Die Datenhaltung erfolgt ueber SQLite, das Backend laeuft mit Deno und Nunjucks als Template-Engine. Das Projekt entstand als Erweiterung der Frontend-Aufgabe aus dem Wintersemester.",
    categories: ["Web", "Backend"],
    tools: ["Deno", "JavaScript", "CSS", "HTML", "Nunjucks", "SQLite"],
    contributors: [],
    images: [
      "/clockworkcms/login.png",
      "/clockworkcms/devlogs.png",
      "/clockworkcms/tutorials.png",
      "/clockworkcms/editor-seite.png",
      "/clockworkcms/editor-blocks.png",
      "/clockworkcms/tipps-neu.png",
    ],
    videos: [],
  },
  {
    id: "leitsystem",
    title: "Leitsystem Hochschule Flensburg",
    year: "2025",
    season: "Sommer",
    semester: "4",
    course: "Konzeption Digitaler Medien",
    description: "Konzeption und Gestaltung eines vollstaendigen Leitsystems fuer den Campus der Hochschule Flensburg. Das System umfasst Wegweiser fuer Aussenbereich und Gaenge, Raumschilder mit Piktogrammen und zweisprachiger Beschriftung (DE/EN), Tuerschilder mit Personenfotos sowie einen neu gestalteten Campusplan und Gebaeudegrundrisse. Farbkonzept und Formen orientieren sich am Corporate Design der Hochschule, die Farben kennzeichnen die einzelnen Gebaeude. Entstanden sind Vorlagen, die als Mockups in Photoshop visualisiert wurden.",
    categories: ["UI", "Print", "Gestaltung"],
    tools: ["Adobe Illustrator", "Adobe InDesign"],
    contributors: ["Leonhard Bünger", "Niclas Merkel", "Berkay-Enes Erdogan","Nikolas Koch", "Merten Hansen"],
    images: [
      "/leitsystem/foto-infotafel.png",
      "/leitsystem/foto-wegweiser.png",
      "/leitsystem/foto-tuerschild.png",
      "/leitsystem/foto-gang.png",
      "/leitsystem/campusmap.png",
      "/leitsystem/tuerschild.png",
      "/leitsystem/wegweiser-gang.png",
      "/leitsystem/raumschild-hs.png",
      "/leitsystem/grundriss-eg.png",
      "/leitsystem/wegweiser-aussen.png",
    ],
    videos: [],
  },
  {
    id: "rendering3d",
    title: "3D Rendering: Motion Tracking",
    year: "2025",
    season: "Sommer",
    semester: "4",
    course: "3D Rendering",
    description: "Integration eines 3D-gerendereten Objekts in reales Videomaterial mittels Motion Tracking. Das Projekt umfasst Camera Tracking zur Rekonstruktion der Kamerabewegung, HDRI-Beleuchtung fuer realistische Licht- und Schattenanpassung, Shader-Entwicklung sowie das abschliessende Compositing. Das gerenderte Objekt wurde nahtlos in eine reale Szene mit Schachbrett eingebettet.",
    categories: ["3D", "Video"],
    tools: ["Blender"],
    contributors: ["Nikolas Koch"],
    images: [
      "/rendering3d/tracking.png",
      "/rendering3d/render.png",
    ],
    videos: ["/rendering3d/tracking.mp4" , "/rendering3d/render.mp4"],
  },
  {
    id: "frontenddesign",
    title: "Clockwork Frontiers: Webseite",
    year: "2025",
    season: "Sommer",
    semester: "4",
    course: "Frontend Design",
    description: "Entwicklung einer vollstaendigen Spielwebseite fuer Clockwork Frontiers als progressive Enhancement ohne Frameworks, mit reinem HTML, CSS und JavaScript. Die Seite umfasst eine Startseite mit Game-Beschreibung und Download-CTA, eine Devlog-Sektion mit chronologisch verlinkten Versionsberichten, eine ausfuehrliche Tutorial-Seite mit Inhaltsverzeichnis und Spielmechanik-Erklaerungen sowie eine Kontaktseite mit Formular. Das visuelle Design greift den isometrischen Look des Spiels auf und verbindet ihn mit einer warmen Retro-Aesthetik, Zahnrad-Motiven und serifenbetonter Typografie.",
    categories: ["UI", "Web"],
    tools: ["HTML", "CSS", "JavaScript"],
    contributors: ["Nikolas Koch"],
    images: [
      "/frontenddesign/screenshot-1.png",
      "/frontenddesign/screenshot-2.png",
      "/frontenddesign/screenshot-3.png",
      "/frontenddesign/screenshot-4.png",
      "/frontenddesign/screenshot-5.png",
      "/frontenddesign/screenshot-6.png",
    ],
    videos: [],
  },
  {
    id: "clockwork",
    title: "Clockwork Frontiers",
    year: "2025",
    season: "Sommer",
    semester: "4",
    course: "Spieleprogrammierung",
    description: "Entwicklung eines 2.5D Tower-Defense-Spiels in Unity nach vorgegebenen Anforderungen. Spieler planen und bauen den Weg fuer ihre Gegner selbst und platzieren Tuerme entlang der Strecke. Clockwork Frontiers entstand im Rahmen des Kurses Spieleprogrammierung und umfasst Leveldesign, Spielmechaniken und 3D-Assets.",
    categories: ["3D", "Game"],
    tools: ["Unity", "C#"],
    contributors: ["Nikolas Koch"],
    images: ["/clockworkwebsite/1.png", "/clockworkwebsite/2.png", "/clockworkwebsite/3.png"],
    videos: ["/clockworkwebsite/demo.mp4"],
  },
  {
    id: "fahrrad3d",
    title: "Fahrrad: 3D-Modellierung",
    year: "2024",
    season: "Winter",
    semester: "3",
    course: "3D-Modellierung",
    description: "Detaillierte 3D-Modellierung von zwei Rennraedern in Blender. Modelliert wurden alle wesentlichen Komponenten: Rahmen, Laufraeder mit Speichen, Kassette, Schaltwerk, Kette, Kurbel, Lenker mit Bremsgriffen sowie Sattel. Besonderes Augenmerk lag auf dem technisch praezisen Antriebsstrang und der authentischen Geometrie beider Raeder. Zusaetzlich wurde eine Bank modelliert, nach selbst aufgenommenen Fotos als Vorlage. Die Modelle wurden in einer gemeinsamen Szene arrangiert und aus mehreren Perspektiven gerendert.",
    categories: ["3D"],
    tools: ["Blender"],
    contributors: ["Nikolas Koch", "Leni Lopau", "Annika Zehms", "Emilia Wink"],
    images: [
      "/fahrrad3d/overview-1.png",
      "/fahrrad3d/overview-2.png",
      "/fahrrad3d/side-front.png",
      "/fahrrad3d/side-back.png",
      "/fahrrad3d/detail-lenker.png",
      "/fahrrad3d/detail-antrieb.png",
      "/fahrrad3d/detail-getriebe.png",
    ],
    videos: [],
  },
  {
    id: "sandsofdeception",
    title: "Sands of Deception: Brettspiel",
    year: "2024",
    season: "Winter",
    semester: "3",
    course: "Game Design",
    description: "Konzept und vollstaendiges Design eines Karten- und Brettspiels fuer 2 bis 4 Spieler. In 'Sands of Deception' schluepfen die Spieler in die Rolle von Forschern, die in einer Pyramide um Artefakte kaempfen, welche als Punktesystem gewertet werden. Das Spieldesign umfasst ein 9-Raum-Labyrinth-Spielbrett, ueber 150 Karten in fuenf Typen (Segen, Monster, Aufgabe, Falle, Saeule), ein Spielertableau zur Aktionspunkte-Verwaltung sowie 3D-modellierte Artefakt-Token in Blender. Entstanden sind vollstaendige Druckvorlagen und ein Regelwerk mit Core-Loop, Aktionen und Kampfsystem.",
    categories: ["Game", "Print", "3D"],
    tools: ["Adobe Illustrator", "Blender"],
    contributors: ["Nikolas Koch", "Moritz Eismann", "Marvin Nischik"],
    images: [
      "/sandsofdeception/umverpackung.png",
      "/sandsofdeception/spielbrett.png",
      "/sandsofdeception/spielertableau.png",
      "/sandsofdeception/karten-segen-vorder.png",
      "/sandsofdeception/karten-monster.png",
      "/sandsofdeception/karten-aufgabe.png",
      "/sandsofdeception/karten-falle.png",
      "/sandsofdeception/anleitung-materialien.png",
      "/sandsofdeception/spielanleitung.png",
      "/sandsofdeception/spielkarten.png",
    ],
    videos: [],
  },
  {
    id: "einkaufsliste",
    title: "WG-Einkaufsliste: App-Prototyp",
    year: "2023",
    season: "Winter",
    semester: "1",
    course: "Interface und Interaktionsdesign",
    description: "Entwicklung und Prototyping einer mobilen Einkaufslisten-App fuer WGs. Die App ermoeglicht gemeinsames Einkaufen in Gruppen mit geteilten Listen, Budgetverwaltung und Kassenbon-Upload. Der Prozess umfasste Konzeption, Lo-Fi-Wireframes mit Interaktionsflows sowie einen vollstaendigen Hi-Fi-Prototyp inkl. Usability-Tests. Features: kategorisierte Einkaufslisten, Sortierfunktion, Finanzen-Uebersicht mit Schuldenausgleich, Pinnwand und Chatfunktion.",
    categories: ["App", "UX", "Prototyp"],
    tools: ["Figma", "Adobe Illustrator"],
    contributors: ["John Owusu-Ankomah", "Moritz Eismann", "Marvin Nischik"],
    images: [
      "/einkaufsliste/hifi-1.png",
      "/einkaufsliste/hifi-2.png",
      "/einkaufsliste/midfi-1.png",
      "/einkaufsliste/midfi-2.png",
      "/einkaufsliste/lofi.png",
      "/einkaufsliste/flowchart.png",
      "/einkaufsliste/konzept.png",
    ],
    videos: [],
  },
  {
    id: "rundgang2024",
    title: "Rundgang 2024: Corporate Design",
    year: "2023",
    season: "Winter",
    semester: "1",
    course: "Grundlagen Gestaltung",
    description: "Entwicklung eines vollstaendigen Corporate Designs fuer den Rundgang 2024 der Hochschule Flensburg unter dem Motto 'Innovation und Erneuerung'. Entstanden sind ein A2-Plakat, ein Dreifach-Flyer sowie eine Einladungskarte, alle aufeinander abgestimmt in Gestaltung, Typografie und Farbsprache. Das visuelle Konzept verbindet ein lebendiges Weltraum-Thema mit klarer Informationsarchitektur: Astronaut, Planeten und Sternbilder als Metaphern fuer Aufbruch und Entdeckung.",
    categories: ["Print", "Gestaltung"],
    tools: ["Adobe InDesign", "Adobe Illustrator", "Adobe Photoshop"],
    contributors: ["Maxim Ekkert", "Nikolas Koch", "Niklas Lohbusch"],
    images: [
      "/rundgang2024/ausstellung.png",
      "/rundgang2024/poster.png",
      "/rundgang2024/flyer-aussen.png",
      "/rundgang2024/flyer-innen.png",
      "/rundgang2024/einladung-aussen.png",
      "/rundgang2024/einladung-innen.png",
    ],
    videos: [],
  },
];

const allCategories = [...new Set(projects.flatMap(p => p.categories))].sort();
const allTools = [...new Set(projects.flatMap(p => p.tools))].sort();
---
<!DOCTYPE html>
<html lang="de" data-theme="light">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Justin Carstensen: Portfolio</title>
  <meta name="description" content="UI Designer & Medieninformatik-Student an der Hochschule Flensburg." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&family=Barlow+Condensed:wght@600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="/styles/global.css" />
</head>
<body>
  <!-- Skip to main content (WCAG 2.4.1) -->
  <a href="#main-content" class="skip-link" data-i18n="skip">Zum Hauptinhalt springen</a>

  <!-- Topbar -->
  <header class="topbar" role="banner">
    <div class="topbar-inner">
      <a href="#" class="logo" aria-label="Justin Carstensen Portfolio Startseite">Justin Carstensen</a>
      <nav class="nav-right" aria-label="Hauptnavigation">
        <a href="#projekte" class="nav-link" data-i18n="nav-projects">Projekte</a>
        <button class="nav-cta" id="contact-btn" aria-haspopup="dialog" data-i18n="nav-contact">Kontakt</button>

        <!-- Language toggle -->
        <div class="lang-toggle" role="group" aria-label="Sprache wählen">
          <button class="lang-btn active" id="lang-de" aria-pressed="true" lang="de">DE</button>
          <button class="lang-btn" id="lang-en" aria-pressed="false" lang="en">EN</button>
        </div>

        <!-- Theme toggle -->
        <button class="theme-toggle" id="theme-toggle" aria-label="Farbschema wechseln" aria-live="polite">
          <span class="theme-toggle-thumb" aria-hidden="true">
            <svg class="sun-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><circle cx="12" cy="12" r="4"/><path d="M12 2v2M12 20v2M4.22 4.22l1.42 1.42M18.36 18.36l1.42 1.42M2 12h2M20 12h2M4.22 19.78l1.42-1.42M18.36 5.64l1.42-1.42"/></svg>
            <svg class="moon-icon" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><path d="M21 12.79A9 9 0 1 1 11.21 3 7 7 0 0 0 21 12.79z"/></svg>
          </span>
        </button>
      </nav>
    </div>
  </header>

  <!-- Contact Modal -->
  <div id="contact-modal" class="contact-modal" hidden role="dialog" aria-modal="true" aria-labelledby="contact-modal-title">
    <div class="contact-modal-bg" id="contact-modal-bg"></div>
    <div class="contact-modal-box">
      <button class="contact-close" id="contact-modal-close" aria-label="Schließen">
        <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg>
      </button>
      <h2 class="contact-modal-title" id="contact-modal-title" data-i18n="contact-title">Kontakt</h2>
      <p class="contact-modal-sub" data-i18n="contact-sub">Schreib mir gerne eine Mail. Ich freue mich auf deine Nachricht.</p>
      <div class="contact-email-row">
        <span class="contact-email-addr" id="contact-email-addr" aria-label="E-Mail-Adresse">justin.carstensen@outlook.de</span>
        <button class="contact-copy-btn" id="contact-copy-btn" aria-live="polite" data-i18n="contact-copy">
          <svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>
          Kopieren
        </button>
      </div>
      <a class="contact-open-btn" id="contact-mail-link" href="mailto:justin.carstensen@outlook.de" data-i18n="contact-open">
        <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
        Mail-Client öffnen
      </a>
    </div>
  </div>

  <!-- Hero -->
  <section class="hero" aria-labelledby="hero-heading">
    <div class="hero-eyebrow" aria-hidden="true">
      <span class="hero-eyebrow-dot"></span>
      <span data-i18n="hero-eyebrow">6. Semester BA Medieninformatik · Hochschule Flensburg</span>
    </div>
    <div class="hero-grid">
      <div class="hero-left">
        <h1 class="hero-name" id="hero-heading">Justin<br /><em>Carstensen</em></h1>
        <p class="hero-sub" data-i18n="hero-sub">
          Medieninformatik-Student im 6. Semester an der HS Flensburg, mit zwei Jahren Erfahrung als SHK im Usability Lab. Mein Schwerpunkt liegt auf UI/UX Design, als auch Websites und Games.
        </p>
      </div>
      <div class="hero-meta">
        <div class="hero-meta-item">
          <strong data-i18n="hero-meta-study">Studium</strong>
          <span data-i18n="hero-meta-university">Hochschule Flensburg</span>
        </div>
        <div class="hero-meta-item">
          <strong data-i18n="hero-meta-focus">Fokus</strong>
          <span data-i18n="hero-meta-focus-val">UI/UX und Websites</span>
        </div>
        <button class="hero-contact-btn" id="hero-contact-btn" aria-haspopup="dialog">
          <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true"><path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/></svg>
          <span data-i18n="hero-contact-btn">E-Mail schreiben</span>
        </button>
      </div>
    </div>
  </section>

  <!-- Filter + Search bar -->
  <div class="filter-wrap" id="projekte">
    <div class="filter-inner" role="search" aria-label="Projekte filtern und suchen">
      <!-- Search -->
      <div class="search-wrap">
        <svg class="search-icon" width="13" height="13" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><circle cx="11" cy="11" r="8"/><line x1="21" y1="21" x2="16.65" y2="16.65"/></svg>
        <input
          class="search-input"
          id="search-input"
          type="search"
          placeholder="Titel oder Kurs suchen…"
          aria-label="Nach Projekttitel oder Kurs suchen"
          autocomplete="off"
          data-i18n-placeholder="search-placeholder"
        />
      </div>

      <div class="filter-sep" aria-hidden="true"></div>

      <!-- Sort -->
      <select class="sort-select" id="sort-select" aria-label="Sortierung wählen">
        <option value="semester-desc" data-i18n="sort-sem-desc">Semester ↓</option>
        <option value="semester-asc" data-i18n="sort-sem-asc">Semester ↑</option>
        <option value="year-desc" data-i18n="sort-year-desc">Jahr ↓</option>
        <option value="year-asc" data-i18n="sort-year-asc">Jahr ↑</option>
      </select>

      <!-- Active filter chips area -->
      <div class="active-filters" id="active-filters" aria-label="Aktive Filter"></div>

      <span class="result-count" id="proj-count" aria-live="polite" aria-atomic="true">
        {projects.length} Projekte
      </span>
    </div>
  </div>

  <!-- Projects -->
  <main id="main-content" aria-label="Projektliste">
    {projects.map((project, i) => (
      <div class="project-item" style={`animation-delay:${i * 60}ms`} data-semester={project.semester} data-year={project.year}>
        <ProjectCard {...project} index={i} />
      </div>
    ))}
  </main>

  <div class="empty-state" id="empty-state" role="status" aria-live="polite">
    <strong data-i18n="empty-title">Keine Projekte gefunden</strong>
    <span data-i18n="empty-sub">Filter oder Suche anpassen, um Ergebnisse zu sehen.</span>
    <br />
    <button class="reset-btn" id="reset-btn" data-i18n="reset-btn">Filter zurücksetzen</button>
  </div>

  <!-- Footer -->
  <footer class="site-footer" role="contentinfo">
    <div class="footer-inner">
      <span class="footer-name">Justin Carstensen</span>
      <span class="footer-right" data-i18n="footer-right">6. Semester BA Medieninformatik · Hochschule Flensburg</span>
    </div>
  </footer>

  <!-- Accessibility Panel -->
  <div class="a11y-knob" id="a11y-knob">
    <div
      class="a11y-panel"
      id="a11y-panel"
      role="dialog"
      aria-modal="false"
      aria-label="Barrierefreiheit-Einstellungen"
      data-i18n-aria="a11y-panel-aria-label"
    >
      <div class="a11y-panel-header">
        <span class="a11y-panel-title" data-i18n="a11y-title">Barrierefreiheit</span>
        <button class="a11y-panel-reset" id="a11y-reset" type="button" data-i18n="a11y-reset">Zurücksetzen</button>
      </div>
      <div class="a11y-panel-body">

        <!-- Vision -->
        <div class="a11y-section">
          <span class="a11y-section-label" data-i18n="a11y-section-vision">Sehen</span>
          <div class="a11y-row">
            <div class="a11y-row-text">
              <span class="a11y-row-label" id="lbl-contrast" data-i18n="a11y-contrast-label">Hoher Kontrast</span>
              <span class="a11y-row-desc" data-i18n="a11y-contrast-desc">Extrem Schwarz/Weiß, keine Farben</span>
            </div>
            <button class="a11y-switch" role="switch" id="a11y-contrast" aria-checked="false" aria-labelledby="lbl-contrast" type="button"></button>
          </div>
          <div class="a11y-row" style="margin-top:10px;">
            <span class="a11y-row-label" id="lbl-fontsize" data-i18n="a11y-fontsize-label">Schriftgröße</span>
            <div class="a11y-btn-group" role="group" aria-labelledby="lbl-fontsize">
              <button class="a11y-opt-btn active" data-fontsize="normal" type="button" data-i18n="a11y-fontsize-normal">Normal</button>
              <button class="a11y-opt-btn" data-fontsize="large" type="button" data-i18n="a11y-fontsize-large">Groß</button>
              <button class="a11y-opt-btn" data-fontsize="larger" type="button" data-i18n="a11y-fontsize-larger">Größer</button>
            </div>
          </div>
          <div class="a11y-row" style="margin-top:10px;">
            <div class="a11y-row-text">
              <span class="a11y-row-label" id="lbl-links" data-i18n="a11y-links-label">Links unterstreichen</span>
              <span class="a11y-row-desc" data-i18n="a11y-links-desc">Alle Links mit Unterstrich</span>
            </div>
            <button class="a11y-switch" role="switch" id="a11y-links" aria-checked="false" aria-labelledby="lbl-links" type="button"></button>
          </div>
        </div>

        <!-- Motor / Lesbarkeit -->
        <div class="a11y-section">
          <span class="a11y-section-label" data-i18n="a11y-section-motor">Motorik &amp; Lesbarkeit</span>
          <div class="a11y-row">
            <div class="a11y-row-text">
              <span class="a11y-row-label" id="lbl-motion" data-i18n="a11y-motion-label">Animationen reduzieren</span>
              <span class="a11y-row-desc" data-i18n="a11y-motion-desc">Alle Transitions deaktivieren</span>
            </div>
            <button class="a11y-switch" role="switch" id="a11y-motion" aria-checked="false" aria-labelledby="lbl-motion" type="button"></button>
          </div>
          <div class="a11y-row" style="margin-top:8px;">
            <div class="a11y-row-text">
              <span class="a11y-row-label" id="lbl-dyslexia" data-i18n="a11y-dyslexia-label">Leseschrift (Legasthenie)</span>
              <span class="a11y-row-desc" data-i18n="a11y-dyslexia-desc">Arial mit mehr Abstand</span>
            </div>
            <button class="a11y-switch" role="switch" id="a11y-dyslexia" aria-checked="false" aria-labelledby="lbl-dyslexia" type="button"></button>
          </div>
        </div>

      </div>
    </div>

    <button
      class="a11y-trigger"
      id="a11y-trigger"
      type="button"
      aria-expanded="false"
      aria-controls="a11y-panel"
      aria-label="Barrierefreiheit-Einstellungen öffnen"
      data-i18n-aria="a11y-trigger-aria-label"
    >
      <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" aria-hidden="true">
        <circle cx="12" cy="4" r="1.5" fill="currentColor" stroke="none"/>
        <path d="M7 8h10M12 8v12M8 12l-2 4M16 12l2 4" stroke-linecap="round" stroke-linejoin="round"/>
      </svg>
    </button>
  </div>

  <script>
    // ── Accessibility Panel ──────────────────────────────────────────────
    (function() {
      const html = document.documentElement;
      const trigger = document.getElementById('a11y-trigger')!;
      const panel   = document.getElementById('a11y-panel')!;
      const PREFS_KEY = 'a11y-prefs';

      type Prefs = { contrast: boolean; fontsize: string; links: boolean; motion: boolean; dyslexia: boolean; };
      const defaults: Prefs = { contrast: false, fontsize: 'normal', links: false, motion: false, dyslexia: false };

      function loadPrefs(): Prefs {
        try { return { ...defaults, ...JSON.parse(localStorage.getItem(PREFS_KEY) || '{}') }; }
        catch { return { ...defaults }; }
      }
      function savePrefs(p: Prefs) { localStorage.setItem(PREFS_KEY, JSON.stringify(p)); }

      function setSwitch(id: string, val: boolean) {
        const btn = document.getElementById(id);
        if (btn) btn.setAttribute('aria-checked', val ? 'true' : 'false');
      }

      function applyPrefs(p: Prefs) {
        // High contrast
        if (p.contrast) html.setAttribute('data-contrast', 'high');
        else html.removeAttribute('data-contrast');

        // Font size
        if (p.fontsize && p.fontsize !== 'normal') html.setAttribute('data-fontsize', p.fontsize);
        else html.removeAttribute('data-fontsize');

        // Links
        if (p.links) html.setAttribute('data-links', 'underline');
        else html.removeAttribute('data-links');

        // Motion
        if (p.motion) html.setAttribute('data-motion', 'reduced');
        else html.removeAttribute('data-motion');

        // Dyslexia
        if (p.dyslexia) html.setAttribute('data-font', 'dyslexia');
        else html.removeAttribute('data-font');

        // Sync UI switches
        setSwitch('a11y-contrast', p.contrast);
        setSwitch('a11y-links',    p.links);
        setSwitch('a11y-motion',   p.motion);
        setSwitch('a11y-dyslexia', p.dyslexia);

        // Sync font size buttons
        document.querySelectorAll<HTMLButtonElement>('[data-fontsize]').forEach(btn => {
          btn.classList.toggle('active', btn.dataset.fontsize === p.fontsize);
        });
      }

      // ── Panel open/close ──────────────────────────────────────────────
      let isOpen = false;

      function openPanel() {
        isOpen = true;
        panel.classList.add('open');
        trigger.setAttribute('aria-expanded', 'true');
      }
      function closePanel() {
        isOpen = false;
        panel.classList.remove('open');
        trigger.setAttribute('aria-expanded', 'false');
      }

      trigger.addEventListener('click', (e) => {
        e.stopPropagation();
        isOpen ? closePanel() : openPanel();
      });

      // Close on outside click
      document.addEventListener('click', (e) => {
        if (isOpen) {
          const knob = document.getElementById('a11y-knob')!;
          if (!knob.contains(e.target as Node)) closePanel();
        }
      });

      // Close on Escape
      document.addEventListener('keydown', (e) => {
        if (e.key === 'Escape' && isOpen) { closePanel(); trigger.focus(); }
      });

      // ── Switch buttons ──────────────────────────────────────────────
      let prefs = loadPrefs();

      function makeSwitch(id: string, key: keyof Prefs) {
        const btn = document.getElementById(id);
        if (!btn) return;
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          (prefs as any)[key] = !(prefs as any)[key];
          applyPrefs(prefs);
          savePrefs(prefs);
        });
      }

      makeSwitch('a11y-contrast', 'contrast');
      makeSwitch('a11y-links',    'links');
      makeSwitch('a11y-motion',   'motion');
      makeSwitch('a11y-dyslexia', 'dyslexia');

      // Font size buttons
      document.querySelectorAll<HTMLButtonElement>('[data-fontsize]').forEach(btn => {
        btn.addEventListener('click', (e) => {
          e.stopPropagation();
          prefs.fontsize = btn.dataset.fontsize!;
          applyPrefs(prefs);
          savePrefs(prefs);
        });
      });

      // Reset
      document.getElementById('a11y-reset')!.addEventListener('click', (e) => {
        e.stopPropagation();
        prefs = { ...defaults };
        applyPrefs(prefs);
        savePrefs(prefs);
      });

      // Auto-detect prefers-reduced-motion on first visit
      if (!localStorage.getItem(PREFS_KEY) && window.matchMedia('(prefers-reduced-motion: reduce)').matches) {
        prefs.motion = true;
      }

      applyPrefs(prefs);
    })();
  </script>

  <script>
    // ── i18n ──────────────────────────────────────────────────────────────
    const TRANSLATIONS: Record<string, Record<string, string>> = {
      de: {
        skip: "Zum Hauptinhalt springen",
        "nav-projects": "Projekte",
        "nav-contact": "Kontakt",
        "hero-eyebrow": "6. Semester BA Medieninformatik · Hochschule Flensburg",
        "hero-sub": "Medieninformatik-Student im 6. Semester an der HS Flensburg, mit zwei Jahren Erfahrung als SHK im Usability Lab. Mein Schwerpunkt liegt auf UI/UX Design, als auch Websites und Games.",
        "hero-meta-study": "Studium",
        "hero-meta-university": "Hochschule Flensburg",
        "hero-meta-focus": "Fokus",
        "hero-meta-focus-val": "UI/UX und Websites",
        "hero-contact-btn": "E-Mail schreiben",
        "contact-title": "Kontakt",
        "contact-sub": "Schreib mir gerne eine Mail. Ich freue mich auf deine Nachricht.",
        "contact-copy": "Kopieren",
        "contact-copied": "Kopiert!",
        "contact-open": "Mail-Client öffnen",
        "filter-cat": "Kategorie",
        "filter-stack": "Stack",
        "search-placeholder": "Titel oder Kurs suchen…",
        "sort-sem-desc": "Semester ↓",
        "sort-sem-asc": "Semester ↑",
        "sort-year-desc": "Jahr ↓",
        "sort-year-asc": "Jahr ↑",
        "empty-title": "Keine Projekte gefunden",
        "empty-sub": "Filter oder Suche anpassen, um Ergebnisse zu sehen.",
        "reset-btn": "Filter zurücksetzen",
        "footer-right": "6. Semester BA Medieninformatik · Hochschule Flensburg",
        "theme-toggle-to-dark": "Zu Dunkel-Modus wechseln",
        "theme-toggle-to-light": "Zu Hell-Modus wechseln",
        "close-contact": "Schließen",
        "project-count-singular": "Projekt",
        "project-count-plural": "Projekte",
        "contact-close-label": "Kontaktfenster schließen",
        "meta-semester": "Semester",
        "meta-period": "Zeitraum",
        "meta-course": "Kurs",
        "attr-stack": "Stack",
        "attr-team": "Team",
        "season-Sommer": "Sommersemester",
        "season-Winter": "Wintersemester",
        "a11y-panel-aria-label": "Barrierefreiheit-Einstellungen",
        "a11y-trigger-aria-label": "Barrierefreiheit-Einstellungen öffnen",
        "a11y-title": "Barrierefreiheit",
        "a11y-reset": "Zurücksetzen",
        "a11y-section-vision": "Sehen",
        "a11y-contrast-label": "Hoher Kontrast",
        "a11y-contrast-desc": "Extrem Schwarz/Weiß, keine Farben",
        "a11y-fontsize-label": "Schriftgröße",
        "a11y-fontsize-normal": "Normal",
        "a11y-fontsize-large": "Groß",
        "a11y-fontsize-larger": "Größer",
        "a11y-links-label": "Links unterstreichen",
        "a11y-links-desc": "Alle Links mit Unterstrich",
        "a11y-section-motor": "Motorik & Lesbarkeit",
        "a11y-motion-label": "Animationen reduzieren",
        "a11y-motion-desc": "Alle Transitions deaktivieren",
        "a11y-dyslexia-label": "Leseschrift (Legasthenie)",
        "a11y-dyslexia-desc": "Arial mit mehr Abstand",
      },
      en: {
        skip: "Skip to main content",
        "nav-projects": "Projects",
        "nav-contact": "Contact",
        "hero-eyebrow": "6th Semester BA Media Informatics · Flensburg University of Applied Sciences",
        "hero-sub": "Media Informatics student in my 6th semester at Flensburg University of Applied Sciences, with two years of experience as a student assistant in the Usability Lab. My focus is on UI/UX design, as well as websites and games.",
        "hero-meta-study": "Studies",
        "hero-meta-university": "Flensburg University of Applied Sciences",
        "hero-meta-focus": "Focus",
        "hero-meta-focus-val": "UI/UX and Websites",
        "hero-contact-btn": "Send an email",
        "contact-title": "Contact",
        "contact-sub": "Feel free to send me an email. I look forward to hearing from you.",
        "contact-copy": "Copy",
        "contact-copied": "Copied!",
        "contact-open": "Open mail client",
        "filter-cat": "Category",
        "filter-stack": "Stack",
        "search-placeholder": "Search by title or course…",
        "sort-sem-desc": "Semester ↓",
        "sort-sem-asc": "Semester ↑",
        "sort-year-desc": "Year ↓",
        "sort-year-asc": "Year ↑",
        "empty-title": "No projects found",
        "empty-sub": "Adjust filters or search to see results.",
        "reset-btn": "Reset filters",
        "footer-right": "6th Semester BA Media Informatics · Flensburg University of Applied Sciences",
        "theme-toggle-to-dark": "Switch to dark mode",
        "theme-toggle-to-light": "Switch to light mode",
        "close-contact": "Close",
        "project-count-singular": "project",
        "project-count-plural": "projects",
        "contact-close-label": "Close contact window",
        "meta-semester": "Semester",
        "meta-period": "Period",
        "meta-course": "Course",
        "attr-stack": "Stack",
        "attr-team": "Team",
        "season-Sommer": "Summer Semester",
        "season-Winter": "Winter Semester",
        "a11y-panel-aria-label": "Accessibility settings",
        "a11y-trigger-aria-label": "Open accessibility settings",
        "a11y-title": "Accessibility",
        "a11y-reset": "Reset",
        "a11y-section-vision": "Vision",
        "a11y-contrast-label": "High contrast",
        "a11y-contrast-desc": "Extreme black/white, no colors",
        "a11y-fontsize-label": "Font size",
        "a11y-fontsize-normal": "Normal",
        "a11y-fontsize-large": "Large",
        "a11y-fontsize-larger": "Larger",
        "a11y-links-label": "Underline links",
        "a11y-links-desc": "Underline all links",
        "a11y-section-motor": "Motor & Readability",
        "a11y-motion-label": "Reduce animations",
        "a11y-motion-desc": "Disable all transitions",
        "a11y-dyslexia-label": "Dyslexia-friendly font",
        "a11y-dyslexia-desc": "Arial with more spacing",
      },
    };

    // Per-project translatable content (title, description, course name)
    const PROJECT_I18N: Record<string, Record<string, { title: string; description: string; course: string }>> = {
      safehaven: {
        en: {
          title: "Project Safe Heaven: Bachelor's Project",
          course: "Bachelor's Project",
          description: "Work in progress. A 2D pixel-art puzzle platformer built in Godot. The game explores mental states, fighting your way out of an inner low, and processing emotions. Different levels represent different emotional worlds, from dark cave systems to calm snowy landscapes. Pixel art assets were created in Aseprite. Made as a bachelor's project in the sixth semester.",
        },
      },
      todoapp: {
        en: {
          title: "Todo App: Web Application",
          course: "Web Apps with TypeScript and React",
          description: "Full-stack todo app built as the exam project for the course 'Web Apps with TypeScript and React'. The app supports multiple todo lists with categories, todos with priority, due dates, tags and sub-todos, bulk actions, an undo function, dark/light mode, multilingual support (DE/EN/FR), and search and filter functionality.",
        },
      },
      jankyjenga: {
        en: {
          title: "Janky Jenga: VR/PC Game",
          course: "Virtual and Augmented Reality",
          description: "Development of an asymmetric multiplayer game in Unity with OpenXR: a VR player uses motion controllers to pull physically simulated Jenga blocks from a tower, while a PC player destabilizes the tower with attacks. The physics simulation is based on realistic rigidbody interactions. 3D models were created in Blender. The project was made as group work and presented with a poster and trailer video.",
        },
      },
      clockworkcms: {
        en: {
          title: "Clockwork Frontiers: CMS",
          course: "Web Programming",
          description: "Extension of the Clockwork Frontiers website with a complete content management system. The CMS provides login and user management, dynamically rendered pages via templating, a block-based page editor, and management of devlogs, tutorials, and tips with search and filter functionality. Data is stored in SQLite, with the backend running on Deno and Nunjucks as the templating engine. The project was built as an extension of the frontend assignment from the winter semester.",
        },
      },
      leitsystem: {
        en: {
          title: "Wayfinding System: Flensburg University of Applied Sciences",
          course: "Digital Media Concept Design",
          description: "Concept and design of a complete wayfinding system for the campus of Flensburg University of Applied Sciences. The system includes outdoor and corridor signage, room signs with pictograms and bilingual labeling (DE/EN), door signs with photos of staff, as well as a newly designed campus map and floor plans. The color concept and shapes follow the university's corporate design, with colors identifying the individual buildings. The result is a set of templates visualized as mockups in Photoshop.",
        },
      },
      rendering3d: {
        en: {
          title: "3D Rendering: Motion Tracking",
          course: "3D Rendering",
          description: "Integration of a 3D-rendered object into real video footage using motion tracking. The project covers camera tracking to reconstruct the camera movement, HDRI lighting for realistic light and shadow matching, shader development, and final compositing. The rendered object was seamlessly embedded into a real scene featuring a chessboard.",
        },
      },
      frontenddesign: {
        en: {
          title: "Clockwork Frontiers: Website",
          course: "Frontend Design",
          description: "Development of a complete game website for Clockwork Frontiers as progressive enhancement without frameworks, using plain HTML, CSS, and JavaScript. The site includes a homepage with a game description and download CTA, a devlog section with chronologically linked version reports, a detailed tutorial page with a table of contents and game mechanic explanations, and a contact page with a form. The visual design picks up the game's isometric look and combines it with a warm retro aesthetic, gear motifs, and serif-driven typography.",
        },
      },
      clockwork: {
        en: {
          title: "Clockwork Frontiers",
          course: "Game Programming",
          description: "Development of a 2.5D tower defense game in Unity according to a fixed set of requirements. Players plan and build the path for their enemies themselves and place towers along the route. Clockwork Frontiers was created as part of the Game Programming course and includes level design, game mechanics, and 3D assets.",
        },
      },
      fahrrad3d: {
        en: {
          title: "Bicycle: 3D Modeling",
          course: "3D Modeling",
          description: "Detailed 3D modeling of two racing bikes in Blender. All essential components were modeled: frame, wheels with spokes, cassette, rear derailleur, chain, crankset, handlebars with brake levers, and saddle. Special attention was paid to a technically precise drivetrain and the authentic geometry of both bikes. A bench was also modeled, based on self-taken reference photos. The models were arranged in a shared scene and rendered from multiple perspectives.",
        },
      },
      sandsofdeception: {
        en: {
          title: "Sands of Deception: Board Game",
          course: "Game Design",
          description: "Concept and complete design of a card and board game for 2 to 4 players. In 'Sands of Deception', players take on the role of explorers fighting over artifacts inside a pyramid, which are scored as a point system. The game design includes a 9-room labyrinth board, over 150 cards across five types (blessing, monster, task, trap, pillar), a player board for managing action points, and 3D-modeled artifact tokens created in Blender. The result is a complete set of print templates and a rulebook covering the core loop, actions, and combat system.",
        },
      },
      einkaufsliste: {
        en: {
          title: "Shared Flat Shopping List: App Prototype",
          course: "Interface and Interaction Design",
          description: "Development and prototyping of a mobile shopping list app for shared flats. The app enables group shopping with shared lists, budget management, and receipt uploads. The process included concept design, lo-fi wireframes with interaction flows, and a complete hi-fi prototype including usability testing. Features: categorized shopping lists, sorting, a finance overview with debt settlement, a pinboard, and a chat function.",
        },
      },
      rundgang2024: {
        en: {
          title: "Open House 2024: Corporate Design",
          course: "Fundamentals of Design",
          description: "Development of a complete corporate design for the 2024 Open House ('Rundgang') at Flensburg University of Applied Sciences under the motto 'Innovation and Renewal'. The result is an A2 poster, a tri-fold flyer, and an invitation card, all coordinated in design, typography, and color language. The visual concept combines a vivid space theme with clear information architecture: astronaut, planets, and constellations as metaphors for new beginnings and discovery.",
        },
      },
    };

    // Category tag display labels (only categories whose English name differs from German)
    const CATEGORY_I18N: Record<string, Record<string, string>> = {
      en: {
        "Gestaltung": "Design",
        "Prototyp": "Prototype",
      },
    };

    let currentLang = localStorage.getItem('lang') || 'de';

    function applyLang(lang: string) {
      currentLang = lang;
      localStorage.setItem('lang', lang);
      document.documentElement.lang = lang;

      const t = TRANSLATIONS[lang];

      // Text elements
      document.querySelectorAll<HTMLElement>('[data-i18n]').forEach(el => {
        const key = el.dataset.i18n!;
        if (t[key]) el.textContent = t[key];
      });

      // Placeholders
      document.querySelectorAll<HTMLInputElement>('[data-i18n-placeholder]').forEach(el => {
        const key = el.dataset.i18nPlaceholder!;
        if (t[key]) el.placeholder = t[key];
      });

      // Aria-labels
      document.querySelectorAll<HTMLElement>('[data-i18n-aria]').forEach(el => {
        const key = el.dataset.i18nAria!;
        if (t[key]) el.setAttribute('aria-label', t[key]);
      });

      // Sort options
      document.querySelectorAll<HTMLOptionElement>('[data-i18n]').forEach(el => {
        if (el.tagName === 'OPTION') {
          const key = el.dataset.i18n!;
          if (t[key]) el.textContent = t[key];
        }
      });

      // Project titles
      document.querySelectorAll<HTMLElement>('[data-i18n-title]').forEach(el => {
        const id = el.dataset.i18nTitle!.replace(/^proj-/, '').replace(/-title$/, '');
        const translated = PROJECT_I18N[id]?.[lang]?.title;
        if (lang === 'de' || !translated) {
          // restore original (German) text only if we previously stored it
          if (el.dataset.origText) el.textContent = el.dataset.origText;
        } else {
          if (!el.dataset.origText) el.dataset.origText = el.textContent || '';
          el.textContent = translated;
        }
      });

      // Project descriptions
      document.querySelectorAll<HTMLElement>('[data-i18n-desc]').forEach(el => {
        const id = el.dataset.i18nDesc!.replace(/^proj-/, '').replace(/-desc$/, '');
        const translated = PROJECT_I18N[id]?.[lang]?.description;
        if (lang === 'de' || !translated) {
          if (el.dataset.origText) el.textContent = el.dataset.origText;
        } else {
          if (!el.dataset.origText) el.dataset.origText = el.textContent || '';
          el.textContent = translated;
        }
      });

      // Project course names
      document.querySelectorAll<HTMLElement>('[data-i18n-course]').forEach(el => {
        const id = el.dataset.i18nCourse!.replace(/^proj-/, '').replace(/-course$/, '');
        const translated = PROJECT_I18N[id]?.[lang]?.course;
        const section = el.closest<HTMLElement>('[data-course]');
        if (lang === 'de' || !translated) {
          if (el.dataset.origText) el.textContent = el.dataset.origText;
        } else {
          if (!el.dataset.origText) el.dataset.origText = el.textContent || '';
          el.textContent = translated;
        }
        if (section) section.dataset.course = el.textContent || '';
      });

      // Project period (season + year), e.g. "Sommersemester 2026" / "Summer Semester 2026"
      document.querySelectorAll<HTMLElement>('[data-i18n-period]').forEach(el => {
        const season = el.dataset.season!;
        const year = el.dataset.year!;
        const seasonWord = t[`season-${season}`] || season;
        el.textContent = `${seasonWord} ${year}`;
      });

      // Category tag labels + filter tooltips
      document.querySelectorAll<HTMLButtonElement>('[data-filter-cat]').forEach(btn => {
        const val = btn.dataset.filterCat!;
        const label = CATEGORY_I18N[lang]?.[val] || val;
        // preserve the active-state span/icon if any by just resetting text content
        btn.textContent = label;
        btn.title = lang === 'de' ? `Nach „${val}" filtern` : `Filter by "${label}"`;
      });

      // Lang buttons
      const deBtn = document.getElementById('lang-de')!;
      const enBtn = document.getElementById('lang-en')!;
      deBtn.classList.toggle('active', lang === 'de');
      enBtn.classList.toggle('active', lang === 'en');
      deBtn.setAttribute('aria-pressed', lang === 'de' ? 'true' : 'false');
      enBtn.setAttribute('aria-pressed', lang === 'en' ? 'true' : 'false');

      // Update count label
      updateCount();
      if (typeof renderChips === 'function') renderChips();
    }

    document.getElementById('lang-de')!.addEventListener('click', () => applyLang('de'));
    document.getElementById('lang-en')!.addEventListener('click', () => applyLang('en'));

    // ── Theme ──────────────────────────────────────────────────────────────
    const root = document.documentElement;
    const themeBtn = document.getElementById('theme-toggle')!;

    function getSystemTheme() {
      return window.matchMedia('(prefers-color-scheme: dark)').matches ? 'dark' : 'light';
    }

    const storedTheme = localStorage.getItem('theme');
    root.setAttribute('data-theme', storedTheme || 'light');

    themeBtn.addEventListener('click', () => {
      const next = root.getAttribute('data-theme') === 'dark' ? 'light' : 'dark';
      root.setAttribute('data-theme', next);
      localStorage.setItem('theme', next);
      const t = TRANSLATIONS[currentLang];
      themeBtn.setAttribute('aria-label', next === 'dark' ? t['theme-toggle-to-light'] : t['theme-toggle-to-dark']);
    });

    // ── Contact modal ──────────────────────────────────────────────────────
    const modal = document.getElementById('contact-modal')!;
    const modalClose = document.getElementById('contact-modal-close')!;
    const modalBg = document.getElementById('contact-modal-bg')!;
    const copyBtn = document.getElementById('contact-copy-btn')!;
    const EMAIL = 'justin.carstensen@outlook.de';

    let lastFocused: HTMLElement | null = null;

    function openModal() {
      lastFocused = document.activeElement as HTMLElement;
      modal.hidden = false;
      document.body.style.overflow = 'hidden';
      // Focus first interactive element
      setTimeout(() => modalClose.focus(), 50);
      trapFocus(modal);
    }

    function closeModal() {
      modal.hidden = true;
      document.body.style.overflow = '';
      lastFocused?.focus();
    }

    // Focus trap inside modal (WCAG 2.1.2)
    function trapFocus(el: HTMLElement) {
      const focusable = el.querySelectorAll<HTMLElement>(
        'button, a[href], input, select, textarea, [tabindex]:not([tabindex="-1"])'
      );
      const first = focusable[0];
      const last = focusable[focusable.length - 1];
      const handler = (e: KeyboardEvent) => {
        if (e.key !== 'Tab') return;
        if (e.shiftKey ? document.activeElement === first : document.activeElement === last) {
          e.preventDefault();
          (e.shiftKey ? last : first).focus();
        }
      };
      el.addEventListener('keydown', handler);
    }

    document.getElementById('contact-btn')!.addEventListener('click', openModal);
    document.getElementById('hero-contact-btn')!.addEventListener('click', openModal);
    modalClose.addEventListener('click', closeModal);
    modalBg.addEventListener('click', closeModal);
    document.addEventListener('keydown', e => { if (e.key === 'Escape' && !modal.hidden) closeModal(); });

    copyBtn.addEventListener('click', async () => {
      try {
        await navigator.clipboard.writeText(EMAIL);
        const t = TRANSLATIONS[currentLang];
        copyBtn.classList.add('copied');
        copyBtn.textContent = t['contact-copied'];
        setTimeout(() => {
          copyBtn.classList.remove('copied');
          const icon = `<svg width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" aria-hidden="true"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>`;
          copyBtn.innerHTML = icon + ' ' + t['contact-copy'];
        }, 2000);
      } catch {
        // fallback: select text
        const range = document.createRange();
        const addr = document.getElementById('contact-email-addr')!;
        range.selectNode(addr);
        window.getSelection()?.removeAllRanges();
        window.getSelection()?.addRange(range);
      }
    });

    // ── Filter + Search + Sort ─────────────────────────────────────────────
    const items = Array.from(document.querySelectorAll<HTMLElement>('.project-item'));
    const countEl = document.getElementById('proj-count')!;
    const emptyEl = document.getElementById('empty-state')!;
    const searchInput = document.getElementById('search-input') as HTMLInputElement;
    const sortSelect = document.getElementById('sort-select') as HTMLSelectElement;
    const mainEl = document.getElementById('main-content')!;
    const resetBtn = document.getElementById('reset-btn')!;
    const activeFiltersEl = document.getElementById('active-filters')!;

    let activeCats = new Set<string>();
    let activeTools = new Set<string>();

    function syncCardTags() {
      // Sync category tags on all project cards
      document.querySelectorAll<HTMLElement>('[data-filter-cat]').forEach(btn => {
        const val = btn.dataset.filterCat!;
        const isActive = activeCats.has(val);
        btn.classList.toggle('active', isActive);
        btn.setAttribute('aria-pressed', isActive.toString());
      });
      // Sync tool tags on all project cards
      document.querySelectorAll<HTMLElement>('[data-filter-tool]').forEach(btn => {
        const val = btn.dataset.filterTool!;
        const isActive = activeTools.has(val);
        btn.classList.toggle('active', isActive);
        btn.setAttribute('aria-pressed', isActive.toString());
      });
    }

    function renderChips() {
      activeFiltersEl.innerHTML = '';
      const mkChip = (type: string, val: string) => {
        const chip = document.createElement('button');
        const label = type === 'category' ? (CATEGORY_I18N[currentLang]?.[val] || val) : val;
        chip.className = 'active-filter-chip';
        chip.dataset.chipType = type;
        chip.type = 'button';
        chip.innerHTML = label + ' <span class="chip-x" aria-hidden="true"><svg width="10" height="10" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="3"><line x1="18" y1="6" x2="6" y2="18"/><line x1="6" y1="6" x2="18" y2="18"/></svg></span>';
        chip.setAttribute('aria-label', currentLang === 'de' ? `Filter '${label}" entfernen` : `Remove filter "${label}"`);
        chip.addEventListener('click', () => {
          if (type === 'category') activeCats.delete(val);
          else activeTools.delete(val);
          syncCardTags();
          renderChips();
          filterAndSort();
        });
        activeFiltersEl.appendChild(chip);
      };
      activeCats.forEach(v => mkChip('category', v));
      activeTools.forEach(v => mkChip('tool', v));
    }

    // Global trigger function used by ProjectCard tags
    (window as any).__triggerFilter = function(type: string, val: string, _btn: HTMLElement) {
      const set = type === 'category' ? activeCats : activeTools;
      if (set.has(val)) set.delete(val);
      else set.add(val);
      syncCardTags();
      renderChips();
      filterAndSort();
      // Scroll to filter bar
      document.getElementById('projekte')?.scrollIntoView({ behavior: 'smooth', block: 'start' });
    };

    searchInput.addEventListener('input', filterAndSort);
    sortSelect.addEventListener('change', filterAndSort);

    resetBtn.addEventListener('click', () => {
      activeCats.clear();
      activeTools.clear();
      searchInput.value = '';
      sortSelect.value = 'semester-desc';
      syncCardTags();
      renderChips();
      filterAndSort();
    });

    function updateCount() {
      const visible = items.filter(i => !i.classList.contains('hidden')).length;
      const t = TRANSLATIONS[currentLang];
      const word = visible === 1 ? t['project-count-singular'] : t['project-count-plural'];
      countEl.textContent = `${visible} ${word}`;
    }

    function filterAndSort() {
      const query = searchInput.value.toLowerCase().trim();

      // Filter
      items.forEach(item => {
        const el = item.querySelector<HTMLElement>('[data-categories]')!;
        const titleEl = item.querySelector<HTMLElement>('.ps-title');
        const cats: string[] = JSON.parse(el.dataset.categories || '[]');
        const tools: string[] = JSON.parse(el.dataset.tools || '[]');
        const title = (titleEl?.textContent || '').toLowerCase();
        const course = (el.dataset.course || '').toLowerCase();

        const catOk = activeCats.size === 0 || [...activeCats].some(c => cats.includes(c));
        const toolOk = activeTools.size === 0 || [...activeTools].some(t => tools.includes(t));
        const searchOk = !query || title.includes(query) || course.includes(query);

        item.classList.toggle('hidden', !(catOk && toolOk && searchOk));
      });

      // Sort
      const sortVal = sortSelect.value;
      const visible = items.filter(i => !i.classList.contains('hidden'));
      visible.sort((a, b) => {
        const semA = parseInt(a.dataset.semester || '0');
        const semB = parseInt(b.dataset.semester || '0');
        const yearA = parseInt(a.dataset.year || '0');
        const yearB = parseInt(b.dataset.year || '0');
        if (sortVal === 'semester-desc') return semB - semA;
        if (sortVal === 'semester-asc') return semA - semB;
        if (sortVal === 'year-desc') return yearB - yearA;
        if (sortVal === 'year-asc') return yearA - yearB;
        return 0;
      });

      // Re-append in sorted order
      visible.forEach(el => mainEl.appendChild(el));
      items.filter(i => i.classList.contains('hidden')).forEach(el => mainEl.appendChild(el));

      const n = visible.length;
      emptyEl.style.display = n === 0 ? 'block' : 'none';
      updateCount();
    }

    // ── Init ───────────────────────────────────────────────────────────────
    applyLang(currentLang);
    filterAndSort();
  </script>
</body>
</html>